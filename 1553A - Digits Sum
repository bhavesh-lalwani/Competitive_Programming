#include <bits/stdc++.h>
using namespace std;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int t,n;
    cin>>t;
    while(t--){
        cin>>n;
        if(n%10 == 9)    cout<< n/10 +1<<"\n";
        else    cout<< n/10 <<"\n";
    }
    return 0;
}
