#include <bits/stdc++.h>
using namespace std;


int main(){
    std::ios::sync_with_stdio(false);    
    cin.tie(NULL);
    int n,times=0;
    cin>>n;
    if(n>=3){
        times=(n/3)*2;
        if(n%3 >= 1)    times++;
    }
    else    times=1;
    cout<<times;
    return 0;
}
